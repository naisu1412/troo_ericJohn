import { DataSource } from "../enum/data_source.enum";

export interface EmployeeDetails {
    first_name: string,
    last_name: string,
    birth_date: string,
    gender: string,
    nationality: string
}